apiVersion: core.oam.dev/v1alpha2
kind: TraitDefinition
metadata:
  name: routes.extend.oam.dev
spec:
  definitionRef:
    name: routes.extend.oam.dev
  extension:
    template: |
      #Template: {
      	apiVersion: "apps/v1"
      	kind:       "Route"
      	spec: {
      		domain: route.domain
      	}
      }

      route: {
      	domain: string
      }
