apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vela-runtime-rollout.fullname" . }}-test-connection"
  labels:
  {{- include "vela-runtime-rollout.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vela-runtime-rollout.fullname" . }}:{{ .Values.healthCheck.port }}']
  restartPolicy: Never