---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: configs.config.oam.dev
spec:
  group: config.oam.dev
  names:
    categories:
    - oam
    kind: Config
    listKind: ConfigList
    plural: configs
    shortNames:
    - cfg
    singular: config
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.templateRef.name
      name: TEMPLATE
      type: string
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Config is the schema for the configs API. It is the CRD-native replacement for
          the legacy labeled-Secret config convention.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConfigSpec defines the desired state of Config.
            properties:
              alias:
                description: Alias is a human-readable name for the config.
                type: string
              description:
                description: Description documents what the config is for.
                type: string
              properties:
                description: |-
                  Properties are inline, non-sensitive properties. Mutually exclusive with
                  PropertiesFrom.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              propertiesFrom:
                description: |-
                  PropertiesFrom sources properties from a Secret, for sensitive values that must
                  not be stored inline in the Config. Mutually exclusive with Properties.
                properties:
                  secretRef:
                    description: SecretRef points at a Secret containing the JSON-encoded
                      properties.
                    properties:
                      key:
                        description: Key within the secret's data. Defaults to "properties".
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - secretRef
                type: object
              templateRef:
                description: |-
                  TemplateRef is the ConfigTemplate (or legacy ConfigMap) this Config is rendered
                  from. If empty, the Config's properties are materialized as-is with no template
                  evaluation.
                properties:
                  name:
                    description: |-
                      Name of the ConfigTemplate (or the suffix of the legacy config-template-<name>
                      ConfigMap).
                    type: string
                  namespace:
                    description: |-
                      Namespace to look up the template in. Defaults to "vela-system" if empty,
                      matching the legacy CLI/factory lookup behavior.
                    type: string
                required:
                - name
                type: object
            type: object
          status:
            description: ConfigStatus defines the observed state of Config.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase is the current lifecycle phase of the Config.
                type: string
              secretRef:
                description: SecretRef is the materialized output Secret, owned by
                  this Config.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
