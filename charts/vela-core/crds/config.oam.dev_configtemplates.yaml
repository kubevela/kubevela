---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: configtemplates.config.oam.dev
spec:
  group: config.oam.dev
  names:
    categories:
    - oam
    kind: ConfigTemplate
    listKind: ConfigTemplateList
    plural: configtemplates
    shortNames:
    - ct
    singular: configtemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.scope
      name: SCOPE
      type: string
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConfigTemplate is the CRD-native replacement for the legacy `config-template-*`
          ConfigMap convention.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConfigTemplateSpec defines the desired state of ConfigTemplate.
            properties:
              alias:
                type: string
              description:
                type: string
              scope:
                default: namespace
                description: ConfigTemplateScope defines where Configs created from
                  this template default to.
                enum:
                - system
                - namespace
                type: string
              sensitive:
                description: |-
                  Sensitive marks Configs created from this template as not safe to read back
                  through the API (e.g. CLI/workflow read operations).
                type: boolean
              template:
                description: |-
                  Template is the CUE template. It must define a `template: {parameter: {...}, output: {...}}`
                  block, matching the format used by the legacy config-template-* ConfigMaps.
                type: string
            required:
            - template
            type: object
          status:
            description: ConfigTemplateStatus defines the observed state of ConfigTemplate.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: ConfigTemplatePhase describes the lifecycle phase of
                  a ConfigTemplate.
                type: string
              schema:
                description: Schema is the OpenAPI v3 schema extracted from template.parameter.
                type: object
                x-kubernetes-preserve-unknown-fields: true
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
