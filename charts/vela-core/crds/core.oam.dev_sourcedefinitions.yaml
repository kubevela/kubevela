---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: sourcedefinitions.core.oam.dev
spec:
  group: core.oam.dev
  names:
    categories:
    - oam
    kind: SourceDefinition
    listKind: SourceDefinitionList
    plural: sourcedefinitions
    shortNames:
    - def-source
    singular: sourcedefinition
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: SourceDefinition is the Schema for sourcedefinitions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SourceDefinitionSpec defines the desired state of SourceDefinition.
            properties:
              restrictions:
                description: |-
                  Restrictions limits where this definition may be used. Absent or empty means
                  usable anywhere. A non-empty block overrides the
                  definition.oam.dev/restrict-namespaces annotation.
                properties:
                  namespaceSelector:
                    description: |-
                      NamespaceSelector selects those namespaces by label instead, for membership
                      that is not a naming convention. An empty selector matches every namespace;
                      leave the field unset to express no opinion.

                      Anyone who can label a namespace can bring it into scope, which a name glob
                      does not allow without the right to create namespaces.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespaces:
                    description: |-
                      Namespaces are the namespaces whose Applications may use this definition, as
                      names or globs ("tenant-*", matched with path.Match).
                    items:
                      type: string
                    type: array
                  quota:
                    description: |-
                      Quota limits how many uses of this definition one namespace may contain. The
                      first entry matching the namespace applies, so an entry with no matcher reads
                      as the default and belongs last. Absent or empty means unlimited.

                      Only ComponentDefinition and TraitDefinition are counted, and a quota on any
                      other kind is rejected when the definition is written. A namespace
                      accumulates components and the traits on them; a policy, workflow step or
                      source is part of how one Application is assembled.

                      The system namespace is never subject to a quota: addons install their own
                      Applications there using these definitions.
                    items:
                      description: |-
                        NamespaceQuota caps how many uses of a definition the namespaces it matches may
                        contain. Its matcher fields carry the same meaning as the ones on
                        DefinitionRestrictions, and an entry with neither matches every namespace.

                        Warn and Limit are both optional, and at least one must be set. Warn alone never
                        refuses, which is how a quota is introduced before it is enforced.
                      properties:
                        limit:
                          description: |-
                            Limit is the most uses of this definition the namespace may contain. Omit to
                            warn without ever refusing; zero forbids the type outright.
                          format: int32
                          minimum: 0
                          type: integer
                        namespaceSelector:
                          description: |-
                            NamespaceSelector selects those namespaces by label instead. It and
                            Namespaces are alternatives, so either one matching selects the entry.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        namespaces:
                          description: |-
                            Namespaces are the namespaces this entry applies to, as names or globs
                            ("tenant-*", matched with path.Match). Leave both matchers unset to make the
                            entry the default.
                          items:
                            type: string
                          type: array
                        warn:
                          description: |-
                            Warn is the count at or above which the Application is admitted with a
                            warning.
                          format: int32
                          minimum: 0
                          type: integer
                      type: object
                    type: array
                type: object
              schematic:
                description: Schematic defines source resolution logic.
                properties:
                  cue:
                    description: CUE defines the encapsulation in CUE format
                    properties:
                      template:
                        description: |-
                          Template defines the abstraction template data of the capability, it will replace the old CUE template in extension field.
                          Template is a required field if CUE is defined in Capability Definition.
                        type: string
                    required:
                    - template
                    type: object
                  terraform:
                    description: Terraform is the struct to describe cloud resources
                      managed by Hashicorp Terraform
                    properties:
                      configuration:
                        description: Configuration is Terraform Configuration
                        type: string
                      customRegion:
                        description: Region is cloud provider's region. It will override
                          the region in the region field of ProviderReference
                        type: string
                      deleteResource:
                        default: true
                        description: DeleteResource will determine whether provisioned
                          cloud resources will be deleted when CR is deleted
                        type: boolean
                      gitCredentialsSecretReference:
                        description: GitCredentialsSecretReference specifies the reference
                          to the secret containing the git credentials
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      path:
                        description: Path is the sub-directory of remote git repository.
                          It's valid when remote is set
                        type: string
                      providerRef:
                        description: ProviderReference specifies the reference to
                          Provider
                        properties:
                          name:
                            description: Name of the referenced object.
                            type: string
                          namespace:
                            default: default
                            description: Namespace of the referenced object.
                            type: string
                        required:
                        - name
                        type: object
                      type:
                        default: hcl
                        description: Type specifies which Terraform configuration
                          it is, HCL or JSON syntax
                        enum:
                        - hcl
                        - json
                        - remote
                        type: string
                      writeConnectionSecretToRef:
                        description: |-
                          WriteConnectionSecretToReference specifies the namespace and name of a
                          Secret to which any connection details for this managed resource should
                          be written. Connection details frequently include the endpoint, username,
                          and password required to connect to the managed resource.
                        properties:
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - configuration
                    type: object
                type: object
              version:
                description: |-
                  Version is the semantic version of this definition. Setting it makes each
                  version a distinct DefinitionRevision, so a binding can pin one with
                  `type: my-source@v1` and stop tracking whatever is newest.
                type: string
            type: object
          status:
            description: SourceDefinitionStatus defines the observed state of SourceDefinition.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configTemplateRef:
                description: ConfigTemplateRef references the ConfigTemplate generated
                  from this SourceDefinition schema.
                properties:
                  name:
                    description: Name is the deterministic template name.
                    type: string
                  schemaHash:
                    description: SchemaHash is the sha256 hash (hex) of the canonical
                      schema cue snippet.
                    type: string
                type: object
              latestRevision:
                description: LatestRevision of this source definition.
                properties:
                  name:
                    type: string
                  revision:
                    format: int64
                    type: integer
                  revisionHash:
                    description: RevisionHash record the hash value of the spec of
                      ApplicationRevision object.
                    type: string
                required:
                - name
                - revision
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
