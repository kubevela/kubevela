# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# Definition source cue file: vela-templates/definitions/internal/configmap-local.cue
apiVersion: core.oam.dev/v1beta1
kind: SourceDefinition
metadata:
  annotations:
    definition.oam.dev/cache-key-rules: 6ac674fa
    definition.oam.dev/cache-key-rules-version: v1
    definition.oam.dev/description: Reads a ConfigMap's data from the Application's own namespace on the cluster being rendered for. Values are strings, as Kubernetes stores them.
  name: configmap-local
  namespace: {{ include "systemDefinitionNamespace" . }}
spec:
  schematic:
    cue:
      template: |
        import "vela/kube"

        // Generated from the context this template reads - do not edit. Admission re-derives these and rejects a mismatch.
        $internal: {
        	key: "configmap-local-\(context.cluster)-\(context.namespace)"
        	keyInputs: ["cluster", "namespace"]
        }
        schema: data: [string]: string

        storage: {
        	storageTTL:     "5m"
        	onStaleFailure: "use-stale"
        }

        parameter: {
        	// +usage=Name of the ConfigMap, read from the Application's own namespace
        	name: string
        }

        _cm: kube.#Get & {
        	$params: {
        		cluster: context.cluster
        		resource: {
        			apiVersion: "v1"
        			kind:       "ConfigMap"
        			metadata: {
        				name:      parameter.name
        				namespace: context.namespace
        			}
        		}
        	}
        }

        output: data: _cm.$returns.data

