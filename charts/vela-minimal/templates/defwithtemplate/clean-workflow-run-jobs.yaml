# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# Definition source cue file: vela-templates/definitions/internal/clean-workflow-run-jobs.cue
apiVersion: core.oam.dev/v1beta1
kind: WorkflowStepDefinition
metadata:
  annotations:
    definition.oam.dev/description: clean addon enable jobs
  labels:
    custom.definition.oam.dev/ui-hidden: "true"
  name: clean-workflow-run-jobs
  namespace: {{ include "systemDefinitionNamespace" . }}
spec:
  schematic:
    cue:
      template: |
        import (
        	"vela/op"
        )

        parameter: labelselector?: {...}
        clean: op.#Delete & {
        	value: {
        		apiVersion: "batch/v1"
        		kind:       "Job"
        		metadata: {
        			name:      context.name
        			namespace: context.namespace
        		}
        	}
        	filter: {
        		namespace: context.namespace
        		if parameter.labelselector != _|_ {
        			matchingLabels: parameter.labelselector
        		}
        		if parameter.labelselector == _|_ {
        			matchingLabels: "workflowrun.oam.dev/name": context.name
        		}
        	}
        }

