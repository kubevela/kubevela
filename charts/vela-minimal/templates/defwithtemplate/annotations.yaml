# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# Definition source cue file: vela-templates/definitions/deprecated/annotations.cue
apiVersion: core.oam.dev/v1beta1
kind: TraitDefinition
metadata:
  annotations:
    definition.oam.dev/description: Add annotations on K8s pod for your workload which follows the pod spec in path 'spec.template'. This definition is DEPRECATED, please specify annotations in component instead.
  name: annotations
  namespace: {{.Values.systemDefinitionNamespace}}
spec:
  appliesToWorkloads:
    - '*'
  podDisruptive: true
  schematic:
    cue:
      template: |
        patch: {
        	metadata: annotations: {
        		for k, v in parameter {
        			"\(k)": v
        		}
        	}
        	spec: template: metadata: annotations: {
        		for k, v in parameter {
        			"\(k)": v
        		}
        	}
        }
        parameter: [string]: string

