apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller
  name: source-controller
  namespace: vela-system
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: source-controller
  type: ClusterIP